name: label_printer
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  # flutter_thermal_printer: ^0.0.16
  provider: ^6.1.2
  printing: ^5.13.2
  pdf: ^3.11.1
  intl: ^0.19.0
  path_provider: ^2.1.4
  file_picker: ^8.1.2
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/fonts/
